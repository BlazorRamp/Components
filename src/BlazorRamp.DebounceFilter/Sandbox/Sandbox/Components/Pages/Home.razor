@page "/"
@using BlazorRamp.DebounceFilter.Common.Models


@rendermode InteractiveServer

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.


<DebounceFilter @ref="control" DebounceDelayMs="300" FilterDataPosition="FilterDataPosition.Centre" ValidationMessage="Invalid entry filtering paused, numbers only"
FilterLabelText="Filter Results" HintText="Data filtered as you type." RegexPattern="[0-9]" OnDebounceFilterResult="HandleDebounce" ></DebounceFilter>

<div>
    Just som text in here to see it move
</div>

<button type="button" @onclick="Clear"">Clear</button>
@code{

    public DebounceFilter control { get; set; }

    public async Task HandleDebounce(DebouncedFilterResult result)
    {

    }


    public async Task Clear()
    {
        await control.ClearFilter();
    }
}
