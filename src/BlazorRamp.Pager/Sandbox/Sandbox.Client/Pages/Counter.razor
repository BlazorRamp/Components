@page "/counter"
@rendermode InteractiveWebAssembly

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>


@* <p>
    <Pager @bind-CurrentPage="buttonCurrentPage" AriaLabel="Min Button" TotalItemCount="100" CurrentItemCount="69" ItemsPerPage="10" PagerSelectorType="PagerSelectorType.Button" ShowFirstLast="false"></Pager>
</p> *@
<p>
    <Pager @bind-CurrentPage="buttonCurrentPage" AriaLabel="Full Button" TotalItemCount="100" CurrentItemCount="69" ItemsPerPage="10" PagerSelectorType="PagerSelectorType.Button" ShowFirstLast="true"></Pager>
</p>
@* <p>
    <Pager @bind-CurrentPage="linkCurrentPage" AriaLabel="Min LInk" TotalItemCount="100" CurrentItemCount="100" ItemsPerPage="10" PagerSelectorType="PagerSelectorType.Link" ShowFirstLast="false"></Pager>
</p>
<p>
    <Pager @bind-CurrentPage="linkCurrentPage" AriaLabel="Full Link" TotalItemCount="100" CurrentItemCount="100" ItemsPerPage="10" PagerSelectorType="PagerSelectorType.Link" ShowFirstLast="true"></Pager>
</p>
 *@
<button @onclick="() => linkCurrentPage = 3">Set to zero</button>




@code {
    private int buttonCurrentPage = 1;
    private int linkCurrentPage = 1;
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}
