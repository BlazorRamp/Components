
<div id="@TabPanelID" role="tabpanel" class="@_cssClasses" aria-labelledby="@TabID">

    @if (_performRender == true)
    {
        if (_isActiveTab || PersistContent)
        {
            @TabPanelContent
        }
    }

</div>

