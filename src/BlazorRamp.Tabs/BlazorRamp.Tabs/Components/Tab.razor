
<div id="@TabPanelID" role="tabpanel" class="@_cssClasses" aria-labelledby="@TabID" tabindex="@(HasPanelTabIndex ? 0 : null)">
    @if (_performRender == true)
    {
        if (_isActiveTab || PersistContent)
        {
            @TabPanelContent
        }
    }
</div>

