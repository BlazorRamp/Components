@page "/"


@rendermode InteractiveServer

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app

<Switch SwitchState="_switchState" SwitchStateChanged="HandleSwitchChange" Label="Airplane mode:" Disabled="_switchDisabled" />
      
<input type="text" value="Paul" />


<button type="button" @onclick="() => _switchDisabled = !_switchDisabled">Toggle Disabled</button>
@code {

    private bool _switchState = false;
    private bool _switchDisabled = false;
    private string _message = String.Empty;

    protected override void OnInitialized()
    {
        _message = $"The switch state is : {(_switchState ? "On" : "Off")}";
    }
    private void HandleSwitchChange(bool switchState)
    {
        _switchState = switchState;
        _message = $"The switch state is : {(switchState ? "On" : "Off")}";
    }

}